snippet     rspec
abbr        RSpec.describe
  require 'rails_helper'

	Rspec.describe ${1:#:described_class}, type: ${2::helper} do
		describe ${3:#:#method_name} do
			${0}
		end
	end

snippet     expect_rwr
abbr        expect receive with return
options     head
	expect(${1:#:object}).to receive(${2:#:message}).with({$3:#:args}).and_return(${4:#:value})
